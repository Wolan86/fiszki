---
import Layout from '@/layouts/Layout.astro';
import { ForgotPasswordForm } from '@/components/auth/ForgotPasswordForm';
---

<Layout title="Odzyskiwanie hasła | Fiszki">
  <div class="container mx-auto py-16 px-4">
    <div class="max-w-md mx-auto">
      <h1 class="text-3xl font-bold text-center mb-8">Odzyskiwanie hasła</h1>
      <ForgotPasswordForm 
        client:load 
        onSubmit={(email) => {
          // Tymczasowa implementacja - będzie zaimplementowana w backendzie
          console.log('Wysłanie linku resetującego do:', email);
          // Symulacja sukcesu
          return { isSuccess: true };
        }}
      />
    </div>
  </div>
</Layout> 